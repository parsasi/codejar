import React from 'react'

export default function RenameFileModalContent(props){
    return(
        <div className="RenameFileModalContainer">
            <h1>Renaming file {props.id}</h1>
        </div>
    )
}